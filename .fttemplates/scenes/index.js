import * as React from 'react';
// view
import { /* selectorView */ /* actionCreators */ } from '../_slice/[FTName | paramCase]'

export default function <FTName | capitalize>(props) {
  // const state = useSelcetor(selectorView)
  // const dispatch = useDispatch()
  // const onClick = () => dispatch(actionCreators('payload'))

  return <FTName | capitalize>Presentational />
}
